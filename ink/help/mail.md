# mail

## This is the postfix-maddy agnostic mail framework
- Checks `ServerMailStatus` and runs `inkemail*` or `inkvmail*`
- Used by the Go inkMail panel at po.emailURI
- BIMI is `ink set bimi`, not a mail schema

## Schemas
Find available schemas with:
- `ink mail -s` or
- `ink mail --schemas`
