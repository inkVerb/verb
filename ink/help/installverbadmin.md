# install verbadmin

## This installs the Go verb web UI on vipURI
- Refuses unless `VERBvip=true` was set before `inst/setup`
- Turning off vipURI means no verb web admin
- Users are created with `ink new verbadmin` or `rink admin verb`, never from the UI

## Usage
- `ink install verbadmin`
