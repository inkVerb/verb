# show

## This displays verber information on the terminal
- `inklog` pages the raw serf output captured by the ink CLI (`ink/log/outputlog`)

## Schemas
Find available schemas with:
- `ink show -s` or
- `ink show --schemas`
