# This file is maintained automatically by inkemailaddscriptfilter, edits could break the file and other structure
## Commented below is the standard entry model for the host domain, with syntax and documentation

# destination unsubscribe@domain286 {
#     check {
#         # Run script
#         command "/srv/vip/email/nativescripts/log_unsubscribe.sh" {sender}
#     }
#     # Discard incoming email
#     deliver_to dummy
# }
# These cannot be on line line, so files containing these are listed in this file

## Below are automated entries, including the active entry for the host domain
## No tab syntax, each unsubscribe entry on one line for single-line entry and deletion
