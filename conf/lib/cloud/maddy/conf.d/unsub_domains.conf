# This file is maintained automatically by inkemaildomain, edits could break the file and other structure
## Commented below is the standard entry model for the host domain, with syntax and documentation

#DEV NOT this:
# source domain.tld {
#     modify {
#         import unsub_headers
#         replace_header {
#             replace List-Unsubscribe-Post "List-Unsubscribe=One-Click"
#             replace List-Unsubscribe "<mailto:unsubscribe@domain.tld>"
#         }
#     } deliver_to &remote_queue
# }

# source domain.tld {
#     modify.headers {
#         add_header List-Unsubscribe-Post ".*" "List-Unsubscribe=One-Click"
#         add_header List-Unsubscribe ".*" "<mailto:unsubscribe@domain.tld>"
#     }
#     deliver_to &remote_queue
# }

## Below are automated entries, including the active entry for the host domain
## No tab syntax, each unsubscribe entry on one line for single-line entry and deletion
