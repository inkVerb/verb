# This file is maintained automatically by inkemailalias, edits could break the file and other structure
## Commented below is the standard entry model for the host domain, with syntax and documentation

# Local alias to another local user
# admin@domain1.tld: postmaster@domain1.tld

# Local alias to an external email address (forwarding)
# forward@domain1.tld: yourname@domain2.tld

# Catch-all (forward everything to one address)
# *@domain1.tld: backup@domain2.tld

## Below are automated entries, including the active entry for the aliases
## No tab syntax, each unsubscribe entry on one line for single-line entry and deletion
