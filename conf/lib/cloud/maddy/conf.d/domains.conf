# Maintained automatically for hosted domains by inkemaildomain
# Maddy requires a value (`$(extra_domains) =` alone fails parse). Seed with primary, not a second zone.
$(extra_domains) = $(primary_domain)
