##
## Plugin settings (Dovecot 2.4)
##

sieve_plugins = sieve_extprograms
sieve_extensions = +vnd.dovecot.filter

sieve_script before {
  path = /etc/dovecot/sieve.before.d/
}

sieve_filter_bin_dir = /etc/dovecot/sieve-filter
sieve_filter_exec_timeout = 120s